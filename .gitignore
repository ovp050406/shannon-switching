# Julia package environment — commit Project.toml, ignore Manifest.toml
# (it pins exact versions and is regenerated by `instantiate`; uebung23 §29)
Manifest.toml

# Julia build / coverage artifacts
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/

# Editor / OS
.vscode/
.DS_Store
